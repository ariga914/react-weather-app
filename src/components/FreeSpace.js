import { React } from "react";

const FreeSpace = () => {
    return (
        <div className="border">
            <h1>Let's attack on TailwindCSS'</h1>
        </div>
    );
};

export default FreeSpace;
